% ============================================================
% RESULT PRESENTATION TEMPLATE - COMPLETE
% For EDC Book 2 - Use for every major result
% Version 1.0 Final - 2026-01-30
% ============================================================

% INSTRUCTIONS:
% 1. Copy this entire template for each major result
% 2. Replace all [PLACEHOLDERS] with actual content
% 3. Delete sections that don't apply (marked as optional)
% 4. Ensure all cross-references (eq:, tab:, sec:) are unique

% ============================================================
% TEMPLATE START
% ============================================================

\subsection{[RESULT NAME]}
\label{sec:[result_label]}

% ═══════════════════════════════════════════════════════════
% GEOMETRIC DERIVATION
% ═══════════════════════════════════════════════════════════

\subsubsection{Derivation Within Model}

\textbf{Geometric setup} [P]:

[Describe the 5D structure:
- Membrane configuration
- Boundary conditions
- Topological features
- Relevant symmetries]

\textbf{Mathematical calculation} yields [Dc:Sym]:
\begin{equation}
  [\text{quantity}] = [\text{exact formula with } \pi, \text{ etc.}]
  \label{eq:[result_formula]}
\end{equation}

\textbf{Derivation sketch:}
\begin{enumerate}
\item [Step 1: Setup/energy functional/action]
\item [Step 2: Variational principle/EOM]
\item [Step 3: Symmetry constraints]
\item [Step 4: Final form]
\end{enumerate}

\textbf{Numerical evaluation:}
\begin{equation}
  [\text{quantity}] = [\text{decimal value to many significant figures}]
\end{equation}

\textbf{Epistemic status}: [Dc:Sym] --- Conditionally derived (symbolic exact)

Conditional on:
\begin{enumerate}
\item 5D postulates [P]: [list specific geometric/topological assumptions]
\item Reduction dictionary: ``[\text{This 5D quantity}] $\equiv$ [\text{PDG observable name}]''
\item Normalization: $C_{\mathrm{red}}^{([\text{subscript}])}$ (currently unconstrained [Open])
\item Approximations: [e.g., ``tree level'', ``leading order in $\alpha$'', ``thin-brane limit'']
\end{enumerate}

% Optional: If numerical integration involved, use [Der:Num] or [Dc:Num]
% and include verification table (see NUMERICAL_VERIFICATION_TEMPLATE.tex)

% ═══════════════════════════════════════════════════════════
% EMPIRICAL COMPARISON
% ═══════════════════════════════════════════════════════════

\subsubsection{Comparison to Measurement}

\textbf{Baseline data} [BL]: Table~\ref{tab:baseline_constants}

PDG/CODATA value:
\begin{equation}
  [\text{quantity}]_{\text{exp}} = [\text{value}] \pm [\text{uncertainty}]
\end{equation}

Source: [PDG 2024 / CODATA 2022 / other]

\textbf{Agreement calculation:}
\begin{align}
\text{EDC prediction:} \quad &[\text{value}_{\text{pred}}] \\
\text{Measurement:} \quad &[\text{value}_{\text{exp}}] \pm [\text{error}] \\
\text{Absolute difference:} \quad &|\Delta| = |[\text{value}_{\text{pred}}] - [\text{value}_{\text{exp}}]| 
                                    = [\text{number}] \\
\text{Relative difference:} \quad &\frac{|\Delta|}{[\text{value}_{\text{exp}}]} 
                                    = [\text{X.XX}]\% = [\text{NNN}]\,\text{ppm}
\end{align}

% ═══════════════════════════════════════════════════════════
% ERROR BUDGET
% ═══════════════════════════════════════════════════════════

\subsubsection{Error Budget}

\begin{table}[h]
\centering
\begin{tabular}{llcc}
\hline
\textbf{Correction Source} & \textbf{Mechanism} & \textbf{Estimate} & \textbf{Status} \\
\hline
EM corrections & $O(\alpha)$ loop effects & $\sim [\text{X}]\%$ & [Open] \\
RG running & $\beta$-functions & $\sim [\text{Y}]\%$ & [Open] \\
Finite-size & $(r/R)^n$ suppression & $\sim [\text{Z}]\%$ & Negligible/[Open] \\
$C_{\mathrm{red}}$ & Normalization factor & Unknown & \textbf{[Open]} \\
[Other source] & [Mechanism description] & $\sim [\text{W}]\%$ & [Open] \\
\hline
\textbf{Total expected} & Sum of estimates & \textbf{$[\text{A}]{-}[\text{B}]\%$} & --- \\
\textbf{Observed} & From comparison above & \textbf{$[\text{C}]\%$} & [BL] \\
\hline
\end{tabular}
\caption{Error budget for [result name]}
\label{tab:error_budget_[result]}
\end{table}

\textbf{Interpretation}:
\begin{itemize}
\item Observed difference $[\text{C}]\%$ is within expected range $[[\text{A}], [\text{B}]]\%$
\item Largest uncertainty: $C_{\mathrm{red}}$ normalization [Open]
\item Agreement is \textbf{strong evidence} for the geometric mechanism
\item NOT conclusive proof until error budget closed (all [Open] terms calculated)
\end{itemize}

% ═══════════════════════════════════════════════════════════
% SENSITIVITY ANALYSIS
% ═══════════════════════════════════════════════════════════

\subsubsection{Sensitivity Analysis}

\textbf{Explored parameter space} (see Section~\ref{sec:epistemic_standard}):

\begin{itemize}
\item $[\text{param}_1]$: varied by $\pm [\text{X}]\%$ 
  $\to$ result shifts by $\pm [\text{Y}]\%$ [linear/quadratic/negligible]

\item $[\text{param}_2]$: factor of 2 variation 
  $\to$ result changes by $[\text{Z}]\%$ [describe dependence]

\item $C_{\mathrm{red}}^{([\text{subscript}])}$: unconstrained [Open] 
  $\to$ \textbf{dominant uncertainty}
\end{itemize}

\textbf{Stability conclusion}:
Within explored ranges (excluding $C_{\mathrm{red}}$), prediction stable to $\pm [\text{W}]\%$.

\textbf{Constraint from agreement}:
Current agreement constrains $C_{\mathrm{red}}^{([\text{subscript}])} \in [[\text{low}], [\text{high}]]$ 
at 95\% confidence level (empirical).

% ═══════════════════════════════════════════════════════════
% STATUS SUMMARY
% ═══════════════════════════════════════════════════════════

\subsubsection{Status Summary}

\begin{center}
\begin{tabular}{ll}
\hline
Mathematical precision: & [Dc:Sym] (symbolic exact) \\
Physical framework: & Incomplete (reduction not fully known) \\
Empirical agreement: & $[\text{C}]\%$ (within expected $[[\text{A}], [\text{B}]]\%$) \\
Epistemic tag: & [Dc:Sym] \\
Confidence level: & High, bounded by stated incompleteness \\
Dominant uncertainty: & $C_{\mathrm{red}}$ normalization [Open] \\
\hline
\end{tabular}
\end{center}

% ═══════════════════════════════════════════════════════════
% FALSIFICATION CRITERIA
% ═══════════════════════════════════════════════════════════

\subsubsection{Tension and Falsification}

\textbf{Current status}:
\begin{itemize}
\item NOT in tension (observed $[\text{C}]\%$ within expected $[[\text{A}], [\text{B}]]\%$)
\item Error budget NOT closed ([Open] corrections remain)
\end{itemize}

\textbf{Would be in tension if}:
\begin{itemize}
\item Future precision measurements shifted by $> [\text{threshold}]\%$
\item After calculating [Open] corrections, residual deviation $> 3\sigma$ measurement uncertainty
\end{itemize}

\textbf{Would be falsified if}:
\begin{itemize}
\item Geometric assumption [specify which] proven incompatible with [specify experiment/test]
\item Multiple predictions simultaneously fail after error budget closure
\item No plausible $C_{\mathrm{red}}$ value can restore agreement across all observables
\end{itemize}

% ═══════════════════════════════════════════════════════════
% PATH FORWARD
% ═══════════════════════════════════════════════════════════

\subsubsection{Path to Higher Certainty}

To strengthen this result:
\begin{enumerate}
\item Complete brane reduction theory (derive $C_{\mathrm{red}}$ from 5D action)
\item Calculate all [Open] corrections explicitly:
  \begin{itemize}
  \item EM corrections: $O(\alpha)$ loop effects
  \item RG running: scale dependence from $\Lambda_{\text{comp}}$ to measurement scale
  \item [Other corrections as listed in error budget]
  \end{itemize}
\item Verify stability under broader parameter variations
\item Independent derivation of dictionary mapping (cross-check)
\item Compare to complementary approaches (if available)
\end{enumerate}

Completing items (1-2) would upgrade [Dc] $\to$ [Der].

% ═══════════════════════════════════════════════════════════
% CROSS-REFERENCES
% ═══════════════════════════════════════════════════════════

\subsubsection{Cross-References}

\begin{itemize}
\item \textbf{Geometric derivation}: Book 1, Chapter [X], Section [Y]; or Chapter [Z] (this work)
\item \textbf{Related results}: Sections~\ref{sec:[related1]}, \ref{sec:[related2]}
\item \textbf{Open problems}: OPR-[number] (see Appendix~\ref{app:opr})
\item \textbf{Baseline data}: Table~\ref{tab:baseline_constants}
\item \textbf{Numerical methods} (if applicable): Section~\ref{sec:numerical_protocol}
\end{itemize}

% ═══════════════════════════════════════════════════════════
% OPTIONAL: 5D MECHANISM BOX
% Include if physical picture needs emphasis
% ═══════════════════════════════════════════════════════════

% \begin{tcolorbox}[colback=blue!5,colframe=blue!75!black,
%                   title=5D Mechanism: [Title]]
% 
% \textbf{Physical Picture in 5D:}
% 
% [Describe what's happening in 5D geometry - concrete, visual description]
% 
% \textbf{Key Geometric Features:}
% \begin{itemize}
% \item Feature 1: [description with parameter values]
% \item Feature 2: [description]
% \end{itemize}
% 
% \textbf{3D Observable Consequence:}
% 
% [What 3D observer sees/measures as result of 5D mechanism]
% 
% \textbf{Why This Works:}
% 
% [Brief physical intuition - why geometric structure yields this prediction]
% 
% \end{tcolorbox}

% ============================================================
% TEMPLATE END
% ============================================================

% CHECKLIST before finalizing:
% [ ] All [PLACEHOLDERS] replaced with actual content
% [ ] Equation labels are unique (eq:[result_formula])
% [ ] Table labels are unique (tab:error_budget_[result])
% [ ] Section labels are unique (sec:[result_label])
% [ ] Cross-references verified
% [ ] Epistemic tags consistent ([Dc:Sym] or [Der:Num], etc.)
% [ ] Error budget has numerical estimates (not just "TBD")
% [ ] Baseline reference points to Table~\ref{tab:baseline_constants}
% [ ] C_red notation used consistently
% [ ] All [Open] items listed in error budget and path forward
