% ============================================================================
% Chapter 3: Metastable Junction
% ============================================================================
% Source: Z3_SYMMETRY_ANALYSIS_NEUTRON.md, V_B_FROM_Z3_BARRIER_CONJECTURE.md
% Status: FILLED from SoT
% Contamination check: PASSED (no SM terms)
% ============================================================================

\chapter{Metastable Junction}
\label{ch:metastable}

\source{Z3\_SYMMETRY\_ANALYSIS\_NEUTRON.md}

\begin{abstract}
The metastable junction decays with a measured lifetime of $\taun \approx 880\second$~[BL].
Why this value? In EDC, the metastable junction is a \emph{metastable configuration} within
the same junction space as the anchor junction---a local minimum of the 5D energy functional
separated from the ground state by an energy barrier. This chapter establishes
the geometric framework: the reaction coordinate $q$, the double-well potential
$V(q)$, and the barrier height conjecture $\VB = 2 \times \Delta m_{np}$.
\end{abstract}

% ----------------------------------------------------------------------------
% CHAPTER SPINE
% ----------------------------------------------------------------------------
\paragraph{Chapter Spine.}
\textbf{Purpose:} Establish the metastable junction as a metastable $\Zthree$-symmetric
configuration in a double-well potential; derive the barrier height $\VB = 2\Delta m_{np}$.
\textbf{Inputs:} (i)~$\Zthree$ subgroup from Ch.~\ref{ch:junction}~[Der],
(ii)~empirical $\taun \approx 880\second$~[BL], (iii)~empirical $\Delta m_{np} \approx 1.293\MeV$~[BL].
\textbf{Outputs:} (i)~Reaction coordinate $q$~[Dc], (ii)~geometric sector
$V_{\mathrm{geom}}(q)$~[Dc] (App.~\ref{app:vq_chosen_path}),
(iii)~double-well $V(q)$ structure~[P],
(iv)~barrier height $\VB = 2\Delta m_{np}$~[P], (v)~branch label convention $s \in \{0,1\}$.
\textbf{Dependencies:} Ch.~\ref{ch:junction} ($\Zthree$ subgroup).
\textbf{Forward links:} $V(q)$ and $\VB$ to Ch.~\ref{ch:instanton} (Euclidean action);
branch label $s$ to Ch.~\ref{ch:helium4} (closed-4 minimality).

% ============================================================================
\section{The $\taun$ Puzzle}
\label{sec:metastable:puzzle}

The metastable junction lifetime is measured with high precision:
\begin{equation}
    \taun \approx 880 \second \tagBL
    \label{eq:tau_n_baseline}
\end{equation}

This is an empirical baseline---a number to be \emph{explained}, not assumed.

\textbf{The puzzle:} Why does the metastable junction persist for this particular duration?
What sets the timescale?

\textbf{EDC perspective:} The metastable junction is not a fundamentally different object
from the anchor junction. Both are Y-junction configurations of 5D worldsheets. The
difference is geometric: the metastable junction occupies a \emph{metastable} configuration---a
secondary minimum of the energy functional, higher than the anchor junction ground state
and separated from it by a barrier.

The decay timescale emerges from quantum tunneling through this barrier. The
goal of Chapters~\ref{ch:metastable}--\ref{ch:tau_n} is to derive $\taun$ from
the geometry of this barrier.

% ============================================================================
\section{$\Zthree$ Configuration as Metastable Branch}
\label{sec:metastable:z3}

\subsection{Configuration Space}

Recall from Chapter~\ref{ch:anchor} that Y-junction configurations form a
space $\mathcal{C}$ parametrized by arm lengths and angles:
\begin{equation}
    \mathcal{C} = \{(L_1, L_2, L_3, \theta_{12}, \theta_{23}, \theta_{31})\}
\end{equation}
with the constraint $\theta_{12} + \theta_{23} + \theta_{31} = 2\pi$.

The $\Zthree$ group acts by cyclic permutation:
\begin{equation}
    g \cdot (L_1, L_2, L_3) = (L_2, L_3, L_1), \quad
    g \cdot (\theta_{12}, \theta_{23}, \theta_{31}) = (\theta_{23}, \theta_{31}, \theta_{12})
\end{equation}

\subsection{Reaction Coordinate $q$}

To describe tunneling between anchor and metastable, we introduce a \emph{collective
coordinate} $q \in [0, 1]$ that parametrizes the deformation path:

\begin{derivationbox}[title=Definition: Reaction Coordinate]
    The reaction coordinate $q$ is a scalar parametrizing the family of
    Y-junction configurations interpolating between anchor and metastable:
    \begin{itemize}
        \item $q = 0$: Anchor configuration (Steiner minimum)
        \item $q = q_n$: Metastable configuration (secondary minimum)
        \item $q = q_B$: Barrier configuration (saddle point)
    \end{itemize}
    \tagDc
\end{derivationbox}

\textbf{Note:} The precise definition of $q$ requires dimensional reduction
from the full 5D action (see Chapter~\ref{ch:instanton}, \S5D$\to$1D reduction).
Here we treat $q$ as an effective coordinate capturing the essential dynamics.

\begin{tcolorbox}[colback=gray!5,colframe=gray!50!black,title=Consistency Note: Continuous $q$ vs.\ Discrete Minima]
Throughout Book IV, $q \in [0, 1]$ is a \emph{continuous} collective coordinate
parametrizing the instanton path. The values $q = 0$ (anchor minimum) and
$q = q_n$ (metastable minimum) are \emph{specific points} on this continuous path,
not discrete labels. When counting junction types in a cluster
(Chapter~\ref{ch:helium4}), we use the \emph{branch label} $s \in \{0, 1\}$:
$s = 0$ for junctions at the anchor minimum, $s = 1$ for junctions at the
metastable minimum. The branch label $s$ is discrete; the coordinate $q$ is
continuous. This distinction avoids conflating the tunneling path with the
endpoint enumeration.
\end{tcolorbox}

\subsection{$\Zthree$-Symmetric vs.\ $\Zthree$-Broken}

A key question is whether the metastable junction preserves or breaks $\Zthree$ symmetry.

\textbf{Order parameter:} The asymmetry is measured by
\begin{equation}
    \Delta = |\Delta_1|^2 + |\Delta_2|^2, \quad
    \Delta_i = L_i - L_{i+1}
\end{equation}
A configuration is $\Zthree$-symmetric if and only if $\Delta = 0$.

\textbf{Observational constraint:} No near-degenerate ``partner'' configurations
are observed that would correspond to a $\Zthree$-multiplet structure. \tagBL

\textbf{Mathematical consequence:} In a $\Zthree$-symmetric system with broken
symmetry, we would expect visible multiplet structure (singlet + doublet states).
The absence of such partners constrains the doublet mode to be stable. \tagM

\begin{resultbox}[title=Metastable junction $\Zthree$ Symmetry]
    Within the EDC framework, the metastable junction configuration is interpreted as
    $\Zthree$-symmetric: the stability coefficient $a(q_n) > 0$ in the
    doublet direction.

    This is a \emph{constraint} from observation [BL]+[M], not a strict
    mathematical proof. \tagDc
\end{resultbox}

% ============================================================================
\section{Double-Well Potential $V(q)$}
\label{sec:metastable:doublewell}

\subsection{Effective Potential}

The 5D dynamics, reduced to the reaction coordinate $q$, gives an effective
potential $V(q)$:

\begin{chapterbox}[title=Postulated Double-Well Structure]
    The potential $V(q)$ is \emph{postulated} to have double-well structure:
    \begin{itemize}
        \item Global minimum at $q = 0$ (anchor) --- geometric sector
              confirms Steiner minimum [Dc]
              (Appendix~\ref{app:vq_chosen_path})
        \item Local minimum at $q = q_n$ (metastable) --- requires
              non-geometric terms, not yet derived \tagP
        \item Local maximum at $q = q_B$ (barrier) --- conditional on
              existence of secondary minimum \tagP
    \end{itemize}
    with $0 < q_B < q_n$. \tagP
\end{chapterbox}

\subsection{Energy Levels}

Taking the anchor junction as reference ($E_p = 0$):

\begin{center}
\begin{tabular}{llll}
\toprule
Configuration & Coordinate & Energy & Symmetry \\
\midrule
Anchor & $q = 0$ & $0$ & $\Zthree$-symmetric (ground) \\
Metastable & $q = q_n$ & $\Delta m_{np}$ & $\Zthree$-symmetric (metastable) \\
Barrier & $q = q_B$ & $E_B$ & $\Zthree$-symmetric (saddle) \\
\bottomrule
\end{tabular}
\end{center}

The barrier height measured from the metastable junction is:
\begin{equation}
    \VB = E_B - \Delta m_{np}
\end{equation}

\subsection{Shape: Partial Geometric Closure}

The geometric brane-energy contribution $V_{\mathrm{geom}}(q)$ has been
computed along two chosen collective paths
(Appendix~\ref{app:vq_chosen_path}). The Nambu--Goto arm-length
calculation gives a \textbf{single-well} profile with a unique minimum
at $q = 0$ (Steiner point):
\begin{equation}
    V_{\mathrm{geom}}(q) = \tau \times L_{\mathrm{tot}}(q), \quad
    V_{\mathrm{geom}}''(0) = \frac{3\tau}{2R} > 0
    \tagDc
\end{equation}
where $\tau$ is the string tension and $R$ the characteristic arm length.
This geometric contribution is the dominant restoring-force term near
the anchor minimum.

The full effective potential along the chosen path is decomposed as:
\begin{equation}
    V(q) = V_{\mathrm{geom}}(q) + V_{\mathrm{node}}(q)
         + V_{\mathrm{bulk}}(q) + \ldots
\end{equation}
The geometric sector $V_{\mathrm{geom}}$ does not produce a secondary
minimum by itself---it is a single well. The double-well structure
(a metastable minimum at $q_n > 0$) requires additional contributions
from junction node energy and bulk effects.

\begin{edcopenbox}[title=Open: Full $V(q)$ from 5D]
    The geometric sector $V_{\mathrm{geom}}(q)$ is now [Dc] along the
    chosen path (Appendix~\ref{app:vq_chosen_path}). What remains open:
    \begin{itemize}
        \item Non-geometric terms ($V_{\mathrm{node}}, V_{\mathrm{bulk}}$)
              that create the secondary minimum --- requires Put~C
              completion (steps C2--C4)
        \item Full double-well structure --- [P], pending non-geometric terms
        \item $V(q)$ functional form from $S_{5\mathrm{D}}$ --- [OPEN]
    \end{itemize}

    \textbf{Status:} Geometric sector = [Dc]. Full $V(q)$ = [P].
    Derivation from $S_{5D}$ = [OPEN].
    \tagOpen
\end{edcopenbox}

% ============================================================================
\section{Barrier Height: $\VB = 2 \times \Delta m_{np}$}
\label{sec:metastable:barrier}

\source{V\_B\_FROM\_Z3\_BARRIER\_CONJECTURE.md}

\subsection{Empirical Input}

The energy difference between metastable and anchor is:
\begin{equation}
    \Delta m_{np} \equiv E_n - E_p \approx 1.293 \MeV \tagBL
    \label{eq:delta_m_np}
\end{equation}
This is an empirical value---the measured energy gap between the two configurations.

\subsection{Numerical Discovery}

Calibration of the tunneling rate to match $\taun \approx 880\second$ gives:
\begin{equation}
    \VB^{\text{cal}} \approx 2.6 \MeV \tagI
\end{equation}

\textbf{Observation:}
\begin{equation}
    \frac{\VB^{\text{cal}}}{\Delta m_{np}} = \frac{2.6}{1.293} \approx 2.01
\end{equation}

This suggests the conjecture: $\VB = 2 \times \Delta m_{np}$.

\subsection{Geometric Motivation}

\begin{derivationbox}[title=Conjecture: $\VB = 2 \times \Delta m_{np}$]
    \textbf{Claim:} The barrier height is twice the metastable junction-anchor energy gap:
    \begin{equation}
        \VB = 2 \times \Delta m_{np} \approx 2.59 \MeV
        \label{eq:VB_conjecture}
    \end{equation}

    \textbf{Argument (in two steps):}

    \emph{Step 1: $\Zthree$ symmetry $\Rightarrow$ factor of 3} \tagDer
    \begin{enumerate}
        \item Define $\Earm$ as the per-arm deformation cost at the barrier
              (a symbolic energy unit, not yet identified with any observable).
        \item The barrier configuration is $\Zthree$-symmetric: all three
              Y-junction arms are equivalently stressed.
        \item By $\Zthree$ symmetry and energy additivity, the total barrier
              energy partitions equally among the three arms:
              \begin{equation}
                  E_B = 3 \times \Earm \tagDer
                  \label{eq:EB_3Earm}
              \end{equation}
        \item The metastable configuration has one effective deformation mode:
              \begin{equation}
                  E_n = 1 \times \Earm \tagDef
                  \label{eq:En_1Earm}
              \end{equation}
        \item Therefore:
              \begin{equation}
                  \VB = E_B - E_n = 3\Earm - 1\Earm = 2\Earm \tagDer
                  \label{eq:VB_2Earm}
              \end{equation}
    \end{enumerate}

    \emph{Step 2: Identification $\Earm \leftrightarrow \Delta m_{np}$} \tagOPEN
    \begin{itemize}
        \item The observer records the metastable-anchor energy gap as
              $\Delta m_{np} \approx 1.293\;\MeV$.
        \item Identifying $\Earm \equiv \Delta m_{np}$ (projection-level
              assumption, not yet derived from 5D action):
              \begin{equation}
                  \VB = 2 \times \Delta m_{np} \approx 2.59\;\MeV \tagP
              \end{equation}
    \end{itemize}

    The factor of 2 follows from $\Zthree$ symmetry [Der]; the numeric
    value requires the identification $\Earm \equiv \Delta m_{np}$ [OPEN].
\end{derivationbox}

\subsection{Physical Picture}

\begin{center}
\begin{tabular}{lll}
\toprule
State & Energy above anchor & Interpretation \\
\midrule
Anchor & $0$ & All arms at equilibrium (Steiner point) \\
Metastable & $1 \times \Earm$ & One effective deformation mode \\
Barrier & $3 \times \Earm$ & All three arms equally stressed \\
\bottomrule
\end{tabular}
\end{center}

\noindent
\emph{Projection note:} $\Earm$ is the per-arm deformation cost in Layer~A.
The observer records the metastable energy as $\Delta m_{np} \approx 1.293\;\MeV$;
identifying $\Earm \equiv \Delta m_{np}$ remains \tagOPEN{} (see \S\ref{sec:metastable:open_unit}).

The factor of 3 at the barrier reflects the $\Zthree$ symmetry: at the saddle
configuration, all three arms carry equal deformation.

\subsection{Remaining Open Step}
\label{sec:metastable:open_unit}

\begin{edcopenbox}[title=Open: Unit Quantization and 5D Origin of $\Earm$]
    The $\Zthree$ symmetry argument (Step~1) derives the factor of 3 from
    symmetry alone. What remains open is the identification $\Earm \equiv \Delta m_{np}$:
    \begin{itemize}
        \item Derive $V(q)$ from the 5D action (bulk+brane+GHY+Israel) along
              the reduction corridor (Put~C). \tagOPEN
        \item Evaluate $V(q_B)$ and verify $E_B = 3\Earm$ at the $\Zthree$-symmetric
              saddle. \tagOPEN
        \item Identify the geometric origin of $\Earm$ and its projection to
              the observer label $\Delta m_{np}$. \tagOPEN
    \end{itemize}

    Until this is derived, the identification $\Earm \equiv \Delta m_{np}$
    remains a projection-level assumption. \tagOPEN

    \textbf{Status:} Unit quantization = [OPEN].
\end{edcopenbox}

% ============================================================================
\section{Preview: Barrier-Limited Transition}
\label{sec:metastable:preview}

The metastable junction undergoes a \emph{barrier-limited transition} to the
anchor configuration. The transition rate is computed using the instanton
formalism in Part~\ref{part:metastable} (Chapters~\ref{ch:instanton}--\ref{ch:tau_n}).

\subsection{What Chapter~\ref{ch:instanton} Establishes}

The effective 1D action along the reaction coordinate:
\begin{equation}
    S_{\text{eff}}[q] = \int dt \left( \frac{1}{2} M(q) \dot{q}^2 - V(q) \right)
\end{equation}

The Euclidean action for tunneling (instanton):
\begin{equation}
    \SE = \kappa \times \frac{\Lzero}{\bdelta} \times \frac{\VB}{E_{\text{scale}}}
\end{equation}

\subsection{What Chapters~\ref{ch:kappa}--\ref{ch:L0delta} Establish}

\begin{itemize}
    \item $\kappa = 2\pi$ from $\pione(\Sone) = \mathbb{Z}$ (homotopy)
    \item $\Lzero/\bdelta \approx \pi^2$ from 5D geometry (hypothesis)
\end{itemize}

\subsection{What Chapter~\ref{ch:tau_n} Computes}

The metastable junction lifetime:
\begin{equation}
    \taun = A \cdot \frac{\hbar}{\omegaz} \cdot \exp(\SE)
\end{equation}
where $A$ is a prefactor and $\omegaz$ is the oscillation frequency at the
metastable minimum.

\textbf{Result preview:} With the parameters from Chapters~\ref{ch:kappa}--\ref{ch:L0delta}
and the barrier height $\VB = 2\Delta m_{np}$, the formula yields $\taun \approx 880\second$
when combined with a calibrated prefactor $A \approx 0.9$~[Cal] and the candidate
ratio $\Lzero/\bdelta = \pi^2$~[P] (see Ch.~\ref{ch:tau_n} for the full epistemic status).

% ============================================================================
\section{Epistemic Status}
\label{sec:metastable:epistemic}

\begin{center}
\begin{tabular}{llll}
\toprule
\textbf{Claim} & \textbf{Status} & \textbf{Reference} & \textbf{Dependency} \\
\midrule
$\taun \approx 880\second$ & [BL] & Eq.~\eqref{eq:tau_n_baseline} & Measurement \\
$\Delta m_{np} \approx 1.293\MeV$ & [BL] & Eq.~\eqref{eq:delta_m_np} & Measurement \\
\midrule
Reaction coordinate $q$ & [Dc] & \S\ref{sec:metastable:z3} & Model reduction \\
Geometric $V_{\mathrm{geom}}(q)$ & [Dc] & App.~\ref{app:vq_chosen_path} & Chosen path \\
Double-well structure & [P] & \S\ref{sec:metastable:doublewell} & Needs non-geom.\ terms \\
Metastable junction $\Zthree$-symmetric & [Dc] & \S\ref{sec:metastable:z3} & [BL]+[M] constraint \\
Barrier $\Zthree$-symmetric & [Dc] & \S\ref{sec:metastable:barrier} & Minimal path \\
$\VB = 2 \times \Delta m_{np}$ & [P] & Eq.~\eqref{eq:VB_conjecture} & Geometric motivation \\
\midrule
$V(q)$ explicit form & \tagOpen & \S\ref{sec:metastable:doublewell} & From 5D action \\
Unit quantization & \tagOpen & \S\ref{sec:metastable:barrier} & ``One unit = $\Delta m_{np}$'' \\
\bottomrule
\end{tabular}
\end{center}

% ============================================================================
\section{Falsifiability}
\label{sec:metastable:falsify}

\begin{edcopenbox}[title=Falsifiability Hooks]
    \begin{enumerate}
        \item \textbf{Barrier existence:} If the 5D action admits no local
              maximum between anchor and metastable minima, the double-well
              picture fails.

        \item \textbf{$\Zthree$ symmetry at barrier:} If the transition
              state breaks $\Zthree$ (unequal arm contributions), the
              factor-of-2 argument is invalidated.

        \item \textbf{Energy quantization:} If the barrier energy is not
              $3 \times \Delta m_{np}$, the geometric picture is wrong.

        \item \textbf{Doublet partners:} If low-lying metastable multiplet
              partners are discovered, the $\Zthree$-symmetric interpretation
              must be revised.
    \end{enumerate}
\end{edcopenbox}

% ============================================================================
% Observer-side projection
% ============================================================================

\begin{observerbox}
Observer-side projection note: quoted terms are measurement labels for the
3D/4D projection of the 5D objects defined in this chapter; they do not
imply any conventional mechanism.

\begin{itemize}
    \item \MetastableJunction{} $\leftrightarrow$ ``neutron'' (projection label)
    \item \AnchorJunction{} $\leftrightarrow$ ``proton'' (projection label)
\end{itemize}

What the observer records: a metastable species with measured lifetime
$\taun \approx 880\second$ and mass excess $\Delta m \approx 1.29\MeV$
relative to the ground-state junction. These are projection-level
observables; the 5D double-well geometry provides the framework for computing them
(see Ch.~\ref{ch:tau_n} for current closure status).
\end{observerbox}

% ============================================================================
\section{Summary}
\label{sec:metastable:summary}

\begin{resultbox}
    The metastable junction is a metastable $\Zthree$-symmetric configuration within the
    Y-junction space:
    \begin{itemize}
        \item Energy: $\Delta m_{np} \approx 1.293\MeV$ above anchor
        \item Barrier height: $\VB = 2 \times \Delta m_{np} \approx 2.59\MeV$ [P]
        \item Decay mechanism: Quantum tunneling through the barrier
    \end{itemize}

    The reaction coordinate $q$ parametrizes the tunneling path. The
    effective potential $V(q)$ has double-well structure [P] with a
    $\Zthree$-symmetric barrier. The physical origin of the non-geometric
    attractive term $V_{\mathrm{node}}(q)$ is [OPEN]: Israel junction
    conditions in the minimal model do not generate it
    (Appendix~\ref{app:P2_WP2_Israel}); bulk backreaction remains untested.

    Chapters~\ref{ch:instanton}--\ref{ch:tau_n} assemble
    $\taun \approx 880\second$ from this geometric framework, subject to one
    [P] hypothesis ($\Lzero/\bdelta = \pi^2$) and one [Cal] prefactor ($A \approx 0.9$).
\end{resultbox}

% ----------------------------------------------------------------------------
% BRIDGE TO NEXT CHAPTER
% ----------------------------------------------------------------------------
\paragraph{Bridge to Chapter~\ref{ch:sigma_to_K}.}
Before developing the instanton calculation, we must establish the other
half of the EDC binding picture: how junctions in a cluster couple to one
another. Chapter~\ref{ch:sigma_to_K} derives the pinning constant $\Kpin$
from brane tension $\bsigma$ and contact geometry---the energy scale that
governs cluster binding energies. This sets up the full energy budget for
bound systems (Ch.~\ref{ch:deuterium}--\ref{ch:helium4}) and ultimately
feeds back into the barrier-tunneling calculation (Ch.~\ref{ch:instanton}).
