% edc_macros.sty — Canonical EDC Symbol Macros
% Date: 2026-03-16
% Authority: Framework v2.0, CANONICAL_NOTATION.md
% Usage: \usepackage{edc_macros} in preamble
%
% This file defines LaTeX macros for all canonical EDC symbols.
% Using these macros ensures notation consistency across all
% EDC books, papers, and documents.
%
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{edc_macros}[2026/03/16 EDC Canonical Notation Macros]

\RequirePackage{amsmath}
\RequirePackage{amssymb}

% ============================================================
% MANIFOLDS AND TOPOLOGY
% ============================================================

% 5D bulk manifold (calligraphic M, NEVER plain M_5)
\newcommand{\Mbulk}{\mathcal{M}^5}

% 4D spacetime
\newcommand{\Mfour}{\mathcal{M}^4}

% 3D brane/membrane (always with superscript)
\newcommand{\brane}{\Sigma^3}

% Circle topology (compact dimension)
\newcommand{\Sone}{S^1}
\newcommand{\Sonexi}{S^1_\xi}

% Higher spheres
\newcommand{\Sthree}{S^3}
\newcommand{\Sfive}{S^5}
\newcommand{\Bthree}{B^3}

% Fundamental group
\newcommand{\pione}{\pi_1}

% ============================================================
% COORDINATES
% ============================================================

% 5D compact coordinate — CANONICAL (never use z or y)
\newcommand{\xifive}{\xi}

% Dimensionless rescaled coordinate (BVP context)
\newcommand{\xitilde}{\tilde{\xi}}

% ============================================================
% LENGTH SCALES
% ============================================================

% Compactification radius — CANONICAL
\newcommand{\Rxi}{R_\xi}

% Orbifold circumference = 2piR_xi
\newcommand{\ellcomp}{\ell}

% Junction spatial extent
\newcommand{\Lzero}{L_0}

% Classical electron radius
\newcommand{\relectron}{r_e}

% Planck length
\newcommand{\ellPl}{\ell_P}

% Junction core thickness (Compton anchor) — NEVER bare delta
\newcommand{\deltaJ}{\delta_J}

% Boundary layer scale — NEVER bare delta
\newcommand{\deltaBL}{\delta_{\mathrm{BL}}}

% Kink half-width (lambda phi^4 domain wall)
\newcommand{\kinkwidth}{\Delta}

% ============================================================
% TENSIONS AND ENERGIES
% ============================================================

% Canonical brane tension [M^4] (covariant)
\newcommand{\sigmabrane}{\sigma}

% Book I surface tension [M^3] (historical)
\newcommand{\sigmasurf}{\sigma_{\mathrm{surf}}}

% Effective membrane tension (Paper 2, calibrated)
\newcommand{\sigmaeff}{\sigma_{\mathrm{eff}}}

% Junction sector tension (8.82 MeV/fm^2)
\newcommand{\sigmajun}{\sigma_{\mathrm{jun}}}

% Cell sector tension (5.86 MeV/fm^2)
\newcommand{\sigmacell}{\sigma_{\mathrm{cell}}}

% Dimensionless brane tension sigma_cov/T_*
\newcommand{\sigmatilde}{\tilde{\sigma}}

% Characteristic tension scale
\newcommand{\Tstar}{T_*}

% Pinning constant
\newcommand{\Kpin}{K}

% Barrier height
\newcommand{\VB}{V_B}

% Ground-state junction energy
\newcommand{\Ezero}{E_0}

% Per-arm deformation energy
\newcommand{\Earm}{E_{\mathrm{arm}}}

% ============================================================
% MASSES
% ============================================================

% 5D Planck mass (NEVER plain M_5 for mass)
\newcommand{\MfivePl}{M_{5,\mathrm{Pl}}}

% Reduced 4D Planck mass
\newcommand{\MPlbar}{\bar{M}_{\mathrm{Pl}}}

% Neutron-proton mass split
\newcommand{\Dmnp}{\Delta m_{np}}

% Unification mass
\newcommand{\MX}{M_X}

% Bulk Dirac mass scale (BVP)
\newcommand{\Mzero}{M_0}

% ============================================================
% COUPLINGS AND CONSTANTS
% ============================================================

% Fine structure constant
\newcommand{\alphaEM}{\alpha}

% Strong coupling at KK scale = 1/sigma_tilde
\newcommand{\alphathree}{\alpha_3}

% 5D gravitational constant
\newcommand{\Gfive}{G_5}

% 4D Newton constant
\newcommand{\GN}{G_N}

% 5D gauge coupling
\newcommand{\gfive}{g_5}

% Corrected 5D gauge coupling (OPR-32)
\newcommand{\gfiveC}{g_5^{(C)}}

% ============================================================
% SYMMETRY GROUPS
% ============================================================

\newcommand{\Zsix}{\mathbb{Z}_6}
\newcommand{\Zthree}{\mathbb{Z}_3}
\newcommand{\Ztwo}{\mathbb{Z}_2}

% ============================================================
% BVP AND WEAK SECTOR
% ============================================================

% Effective potentials (SUSY QM pair)
\newcommand{\VeffL}{V_L}
\newcommand{\VeffR}{V_R}

% Robin BC parameter = m_b/2
\newcommand{\kappaBC}{\kappa_{\mathrm{BC}}}

% Penetration depth inverse
\newcommand{\kappapen}{\kappa_{\mathrm{pen}}}

% Junction geometric factor = 5/6
\newcommand{\kappathreeq}{\kappa_{3q}}

% Dimensionless BVP parameter = M_0 * ell
\newcommand{\muBVP}{\mu}

% Shape parameter = Delta/ell
\newcommand{\rhoBVP}{\rho}

% Weak mixing angle
\newcommand{\sintwW}{\sin^2\!\theta_W}

% ============================================================
% NEUTRON LIFETIME (PATH B)
% ============================================================

% Neutron lifetime
\newcommand{\taun}{\tau_n}

% Euclidean bounce action
\newcommand{\SEuc}{S_E}

% Attempt frequency
\newcommand{\omegazero}{\omega_0}

% ============================================================
% NUCLEAR TOPOLOGY (BOOK IV)
% ============================================================

% Effective coordination number
\newcommand{\ncoord}{n(A)}

% Coordination prefactor
\newcommand{\pcoord}{p}

% Frustration distance to allowed set
\newcommand{\dfrust}{d(n)}

% GN barrier ratio
\newcommand{\XGN}{X}

% Frustration coupling coefficient
\newcommand{\gfrust}{g}

% ============================================================
% SOLITON PROFILE
% ============================================================

% Brane displacement profile
\newcommand{\fsol}{f(r)}

% Golden ratio tail exponent
\newcommand{\goldenratio}{\varphi}

% ============================================================
% ACTIONS
% ============================================================

% Total EDC action
\newcommand{\SEDC}{S_{\mathrm{EDC}}}

% Component actions
\newcommand{\Sbulk}{S_{\mathrm{bulk}}}
\newcommand{\Sbrane}{S_{\mathrm{brane}}}
\newcommand{\Sdefect}{S_{\mathrm{defect}}}
\newcommand{\SGHY}{S_{\mathrm{GHY}}}
\newcommand{\Seff}{S_{\mathrm{eff}}}

% Bulk-to-brane exchange current (Framework v2.0)
\newcommand{\Jbb}[1]{J^{#1}_{\mathrm{b}\to\mathrm{b}}}

% ============================================================
% CANONICAL FORMULAS
% ============================================================

% Proton-electron mass ratio
\newcommand{\mpratio}{6\pi^5}

% Fine structure constant formula
\newcommand{\alphaformula}{\frac{4\pi + \tfrac{5}{6}}{6\pi^5}}

% Bounce exponent
\newcommand{\bounceexp}{2\pi^3}

% ============================================================
% EPISTEMIC TAGS
% ============================================================

% Tags (text mode, sans-serif)
\newcommand{\tagDer}{\textsf{[Der]}}
\newcommand{\tagDc}{\textsf{[Dc]}}
\newcommand{\tagP}{\textsf{[P]}}
\newcommand{\tagCal}{\textsf{[Cal]}}
\newcommand{\tagBL}{\textsf{[BL]}}
\newcommand{\tagI}{\textsf{[I]}}
\newcommand{\tagM}{\textsf{[M]}}
\newcommand{\tagDef}{\textsf{[Def]}}
\newcommand{\tagOPEN}{\textsf{[OPEN]}}
\newcommand{\tagFALSIFIED}{\textsf{[FALSIFIED]}}
\newcommand{\tagNEGATIVE}{\textsf{[NEGATIVE]}}

% ============================================================
% EDC-NATIVE ONTOLOGY (Book IV Layer A)
% ============================================================

\newcommand{\AnchorJunction}{anchor junction}
\newcommand{\MetastableJunction}{metastable junction}
\newcommand{\LoopState}{loop state}
\newcommand{\EdgeMode}{edge-mode excitation}
\newcommand{\ClusterState}{cluster state}
\newcommand{\ClosedFour}{closed-4 unit}

\endinput
